# Phase 0. Dependencies are added per phase rather than up front, so that
# what is installed always reflects what is actually built.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
